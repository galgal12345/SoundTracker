<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drillgil.android.audiomusicplayer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AudioMusicPlayer">
        <activity android:name=".Study.StudyPlayer"></activity>
        <activity android:name=".Study.StudyAdapter" />
        <activity android:name=".GoodVibes.GoodVibesPlayer" />
        <activity android:name=".GoodVibes.GoodVibesAdapter" />
        <activity android:name=".Meditate.MeditatePlayer" />
        <activity android:name=".Meditate.MeditateAdapter" />
        <activity android:name=".OnTheWay.OnTheWayPlayer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>